# 🍳 AI Промо Готвач (Kaufland Chef)

Това е уеб приложение на Python (Flask), което служи като Proof-of-Concept (PoC) за "умен" кулинарен асистент.

Приложението автоматично извлича (scrape-ва) актуалните промоции от Kaufland.bg, записва ги в локална база данни (SQLite) и след това използва Google Gemini AI, за да генерира рецепти в реално време, базирани на:
* Наличните промоции.
* Продуктите, които потребителят вече има в хладилника.
* Персонализирани филтри (бюджет, алергии, вегетарианско и др.).

## 🚀 Технологичен Стек

* **Backend:** Python
* **Web Framework:** Flask
* **AI Модел:** Google Gemini (през `google-generativeai`)
* **База Данни:** SQLite (за съхранение на продукти)
* **Web Scraper:** Requests & BeautifulSoup4
* **Автоматизация (Scheduler):** APScheduler (за ежедневно обновяване на базата данни)
* **Управление на API ключове:** `python-dotenv`

## ⚙️ Стъпки за стартиране (Локално)

Ето как да стартирате проекта на вашата машина:

### 1. Клониране на Репозиторито

```bash
git clone [https://github.com/ТВОЕТО_ИМЕ/ТВОЕТО_РЕПО.git](https://github.com/ТВОЕТО_ИМЕ/ТВОЕТО_РЕПО.git)
cd ТВОЕТО_РЕПО
```

### 2. Създаване и Активиране на Виртуална Среда

```bash
# Създаване (използвайки твоята инсталация на Python 3.9+)
py -m venv .venv

# Активиране (Windows)
.\.venv\Scripts\activate
```

### 3. Инсталиране на Зависимостите

Проектът използва `requirements.txt` за управление на библиотеките.

```bash
pip install -r requirements.txt
```

### 4. Настройка на API Ключ (Критично)

Проектът изисква API ключ от Google AI Studio (Gemini).

1.  Отидете на [Google AI Studio](https://aistudio.google.com/app/apikey) и си генерирайте ключ.
2.  Създайте **нов файл** в главната директория на проекта с име **`.env`**.
3.  Отворете `.env` файла и добавете **един** ред, като замените `...` с вашия ключ:
    ```text
    GEMINI_API_KEY="AIzaSy...ТВОЯТ_НОВ_КЛЮЧ_ТУК"
    ```
4.  Файлът `.gitignore` вече е настроен да игнорира `.env`, така че ключът ви е в безопасност.

### 5. Стартиране на Приложението

След като средата е активна и ключът е поставен, стартирайте Flask сървъра:

```bash
python app.py
```
При първото стартиране, скриптът автоматично ще стартира scraper-а, за да напълни базата данни `products.db`. Това може да отнеме 10-15 секунди.

### 6. Отворете Приложението

Отворете вашия уеб браузър и отидете на:

**`http://127.0.0.1:5000`**

---
*Забележка: Web scraper-ът (`logic.py`) е настроен да търси специфични HTML класове на сайта на Kaufland. Ако приложението спре да намира продукти, това е защото Kaufland са актуализирали HTML структурата на сайта си и scraper-ът трябва да бъде обновен.*